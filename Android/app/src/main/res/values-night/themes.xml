<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Night (DARK) override. Colors come from values-night/colors.xml via
         the shared resource names; this only flips status-bar icon contrast. -->
    <style name="Theme.CapitalWizard" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/accent</item>
        <item name="colorPrimaryVariant">@color/accent</item>
        <item name="colorOnPrimary">@color/on_accent</item>
        <item name="colorSecondary">@color/accent</item>
        <item name="colorSecondaryVariant">@color/accent</item>
        <item name="colorOnSecondary">@color/on_accent</item>
        <item name="android:colorBackground">@color/background</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="colorError">@color/error_red</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <!-- Dark background → light status-bar icons -->
        <item name="android:windowLightStatusBar">false</item>
    </style>

</resources>
